import React, { Component } from 'react';
import './Home.css';

const styles = theme => ({});

class Home extends Component {
    render() {
        return (
            <div>
                Home page
            </div>
        )
    }
}

export default withStyles(styles)(Home);
